# 테스트는 개발자의 .env.local(실제 Supabase 키)과 무관하게 항상 게스트 모드로 돌아야 한다.
# 안 그러면 로컬에 .env.local이 있는 사람만 테스트가 실제 네트워크 클라이언트를 만들다가
# 깨지고(Node의 WebSocket 미지원 등), CI처럼 .env.local이 없는 환경과 동작이 달라진다.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
